# API Configuration (removed - using /api.php at root)
# Frontend automatically uses /api.php endpoint

# Node Environment
NODE_ENV=development

# These variables are handled by your Apache/PHP server, not Node.js
# Admin credentials are managed on the server side only
