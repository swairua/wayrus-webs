# Environment Configuration
# Frontend automatically uses /api.php endpoint (no NODE_ENV needed)
# All admin credentials and sensitive data are managed by Apache/PHP server
# Do not commit .env files with secrets - they are in .gitignore

NODE_ENV=development
